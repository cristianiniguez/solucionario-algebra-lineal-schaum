# CAPITULO 1 - SISTEMAS DE ECUACIONES LINEALES

## MATRICES ESCALONADAS Y OPERACIONES ELEMENTALES ENTRE FILAS

1.59. Demostrar que la equivalencia por filas es una relación de equivalencia:

i. $A$ es equivalente por filas a $A$.

Una matriz se puede transformar en sí misma realizando cero operaciones elementales (o la identidad de operaciones). Entonces $A \sim A$

ii. Si $A$ es equivalente por filas a $B$, entonces $B$ es equivalente por filas a $A$.

Si $A \sim B$, entonces existe una sucesión de operaciones elementales entre filas que transforma $A$ en $B$.

Cada operación elemental tiene una inversa del mismo tipo.

Entonces, aplicando las operaciones inversas en orden inverso, se puede transformar $B$ nuevamente en $A$. Por lo tanto, $B \sim A$.

iii. Si $A$ es equivalente por filas a $B$ y $B$ es equivalente por filas a $C$, entonces $A$ es equivalente por filas a $C$.

Si $A \sim B$ y $B \sim C$, entonces existe una sucesión de operaciones elementales entre filas que transforma $A$ en $B$ y otra que transforma $B$ en $C$.

Al componer estas dos sucesiones de operaciones elementales, se obtiene una sucesión de operaciones elementales que transforma $A$ en $C$. Por lo tanto, $A \sim C$.
